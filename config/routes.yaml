dashboard:
    path: /
    controller: App\Controller\Application::index

login:
    path: /login
    controller: App\Controller\Application::index

login_check:
    path: /login_check

logout:
    path: /logout
    
appConfig:
    path: /config
    controller: App\Controller\Application::config
    methods: GET
    
report:
    resource: 'routes/report.yaml'
    prefix: /report
    
machines:
    prefix: /machines
    resource: 'routes/machines.yaml'    